@model IEnumerable<Model.DTO.WebShopDTO>
@{
    ViewBag.Title = "WebShops";
   
}



<br/>
<br/>
<br/>
<br/>
<div id="modDialog" class="modal fade">
    <div id="dialogContent" class="modal-dialog"></div>
</div>

<div class="container">
    <div class="col-md-12">
        <div class="col-md-1 col-md-offset-10">
            <a href="@Url.Action("Create")" class="btn btn-primary btn-md">
                <span class="glyphicon-plus"></span> Add Item
            </a>
        </div>
    </div>
    <br/>
    <br/>
    <div class="col-md-12">
        <table class="table">
            <tr>
                <th>
                    LOGO
                </th>
                <th>
                    NAME
                </th>
                <th>
                    URL
                </th>
                <th>

                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <img class="Logo" src="~/Content/WebShopsLogo/@item.LogoPath"/>
                    </td>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        <a href="@item.Path">Go to</a>
                    </td>
                    <td>
                        @Html.ActionLink("Delete", "Delete", new {id = item.Id}, new {@class = "compItem"})
                        @*@Html.ActionLink("Details", "Details", new {id = item.Id}, new {@class = "compItem btn btn-primary btn-xs"})*@
                    </td>
                </tr>
            }
        </table>
    </div>
</div>

@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/WebShops")

@section scripts
{
    <script type="text/javascript">

        $(function() {
            $.ajaxSetup({ cache: false });
            $(".compItem").click(function(e) {

                e.preventDefault();
                $.get(this.href, function(data) {
                    $('#dialogContent').html(data);
                    $('#modDialog').modal('show');
                });
            });
        });
    </script>
}           
